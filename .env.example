# The two FIXED services. Agents are DISCOVERED at runtime — never configured here.
# Copy to .env (which is gitignored) and adjust for your stack.

VITE_DISCOVERY_URL=http://localhost:3005
VITE_IDSERVER_URL=http://localhost:3001
